from fastapi import APIRouter

router = APIRouter()

# Future API routes will be registered here
# Example: from app.api.endpoints import some_router
#          router.include_router(some_router, prefix="/some-prefix")
